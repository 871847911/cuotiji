<!--pages/lisen/lisen.wxml-->
<button type='' bindtap='sendMessage'>播放</button>
<!-- <audio src="{{filePath}}" id="myAudio" ></audio> -->
<view wx:for="{{msg}}" wx:key="{{index}}">
    {{index}}:{{item.words}}
    <button type="primary" data-index="{{index}}" bindtap="sendMessage">播放</button>
</view>
